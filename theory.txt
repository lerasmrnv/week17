1. Можно использовать '"`, используя ` можно писать произвольные выражения. В одинарных ковычках, можно использовать двойные и наоборот, но нельзя использовать """" и ''''.
2. Можно использовать метод .indexOf() и .lastIndexOd(), а также .search()
3. Это символ для перевода строки на новую строчку
4. 
let username = "настя";
let usernameUpper = username.charAt(0).toUpperCase() + username.slice(1);
console.log(usernameLower);
5. let date = new Date(2021, 0, 24, 22, 51,0);
6. let date = new Date();
console.log(date.getTime()); или console.log(+date);
7. Необходимо использовать parseInt(el), функция превращает строку в число.
8. .join - склеивает из массива строку
.split наоборот преобразовывает строку в массива
9. Math.round()
10. Math.round(N]Math.random()*100)
11. Убирает пробелы в начале и в конце строки
12. флаги, позволяют влиять на поиск. К примеру, \i позволяет искать не учитывая регистр
